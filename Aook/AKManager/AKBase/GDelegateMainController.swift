
import Foundation

import UIKit

class GDelegateMainController: UITabBarController {
private var displayPromptGraphicsString: String?
private var delegate_9x: Double = 0.0
private var checkNav_string: String?
private var prefix_0Pic_Array: [Any]!

@discardableResult
 func mixFillVersionCard() -> Int {
    var value4: String! = String(cString: [101,120,112,105,114,121,0], encoding: .utf8)!
    var descriptj: String! = String(cString: [114,101,103,101,116,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      descriptj.append("\(value4.count << (Swift.min(labs(3), 5)))")
   }
      value4 = "\(descriptj.count + 2)"
   while (value4.count == 1) {
       var w_widthq: Double = 0.0
      for _ in 0 ..< 3 {
          var phonec: String! = String(cString: [106,95,51,54,95,119,114,105,116,101,99,98,0], encoding: .utf8)!
         w_widthq += (Double(phonec == (String(cString:[97,0], encoding: .utf8)!) ? Int(w_widthq) : phonec.count))
      }
      while (1.32 == (w_widthq + w_widthq)) {
         w_widthq += Double(Int(w_widthq))
         break
      }
         w_widthq *= Double(Int(w_widthq))
      descriptj = "\(((String(cString:[97,0], encoding: .utf8)!) == value4 ? descriptj.count : value4.count))"
      break
   }
      descriptj.append("\(descriptj.count)")
     let channelShared: String! = String(cString: [111,95,56,51,95,99,104,111,111,115,101,114,0], encoding: .utf8)!
     let removeFast: Double = 9583.0
    var biweightDinersMaskq:Int = 0
    biweightDinersMaskq *= Int(removeFast)

    return biweightDinersMaskq

}









    override func viewDidLoad() {

         let nalusMplame: Int = mixFillVersionCard()

      if nalusMplame < 36 {
             print(nalusMplame)
      }



       var cancel8: String! = String(cString: [118,102,105,108,116,101,114,0], encoding: .utf8)!
    var clearp: String! = String(cString: [120,99,111,100,101,0], encoding: .utf8)!
   while (3 < cancel8.count) {
      clearp = "\((cancel8 == (String(cString:[104,0], encoding: .utf8)!) ? cancel8.count : clearp.count))"
      break
   }

   while (2 < cancel8.count) {
      clearp = "\(3)"
      break
   }
        super.viewDidLoad()
   while (clearp.count == cancel8.count) {
      cancel8.append("\(clearp.count ^ 2)")
      break
   }
        
        tabBar.barTintColor = .clear
        tabBar.backgroundColor = UIColor(red: 11/255, green: 12/255, blue: 21/255, alpha: 1.0)
        tabBar.tintColor = UIColor(red: 11/255, green: 12/255, blue: 21/255, alpha: 1.0)

        let levelController = [
            [FNLModityController(), "聊天", "popupMineJiao", "playSureYinsi"],
            [HIScreenController(), "助手", "openCloseFirst", "lineClass_dnLiji"],
            [MJOShowRingController(), "角色", "changeSearchMain", "aboutFiveRing"],
            [XJFMenuController(), "记录", "collectionClass_9", "centerModityCopy__p"]
        ]
        
        for item in levelController {

            let z_playerItem: UITabBarItem = UITabBarItem(title: item[1] as? String, image: UIImage(named: item[2] as! String), selectedImage: UIImage(named: item[3] as! String)?.withRenderingMode(.alwaysOriginal))
                let screenController = SSYBufferController(rootViewController: item[0] as! UIViewController)
                
                let big: [NSAttributedString.Key: Any] = [
                    .foregroundColor: UIColor(red: 82/255, green: 82/255, blue: 82/255, alpha: 1.0),
                    .font: UIFont.systemFont(ofSize: 12)
                ]
                
                let silence: [NSAttributedString.Key: Any] = [
                    .foregroundColor: UIColor(red: 205/255, green: 205/255, blue: 205/255, alpha: 1.0),
                    .font: UIFont.boldSystemFont(ofSize: 12)
                ]
            
                z_playerItem.setTitleTextAttributes(big, for: .normal)
                z_playerItem.setTitleTextAttributes(silence, for: .selected)
                z_playerItem.titlePositionAdjustment = UIOffset(horizontal: 0, vertical: 3)
                screenController.tabBarItem = z_playerItem
                
                self.addChild(screenController)
        }
    }
    
    override var preferredStatusBarStyle: UIStatusBarStyle {
       var verityP: Float = 2.0
    var relationY: [Any]! = [967, 228, 830]
      verityP *= Float(relationY.count - 2)
   while ((relationY.count + 5) == 5 && 2.6 == (verityP * 1.15)) {
      relationY = [3]
      break
   }

        return .lightContent
   if 2 <= (relationY.count + 1) {
      relationY = [1]
   }
    }

    
    override func viewWillAppear(_ animated: Bool) {
       var descriptz: String! = String(cString: [99,108,97,115,115,105,102,121,0], encoding: .utf8)!
    var completionO: String! = String(cString: [115,98,111,120,0], encoding: .utf8)!
    var prefix_s7u: Double = 0.0
      descriptz = "\(((String(cString:[68,0], encoding: .utf8)!) == completionO ? descriptz.count : completionO.count))"
   while (descriptz.count >= 5) {
      prefix_s7u /= Swift.max(Double(1), 5)
      break
   }
   repeat {
      prefix_s7u += Double(descriptz.count * 3)
      if 1038803.0 == prefix_s7u {
         break
      }
   } while (1038803.0 == prefix_s7u) && ((5.58 + prefix_s7u) > 5.5)

   repeat {
      completionO.append("\(completionO.count)")
      if 1566563 == completionO.count {
         break
      }
   } while (prefix_s7u == 1.15) && (1566563 == completionO.count)
   for _ in 0 ..< 1 {
      completionO = "\((completionO == (String(cString:[79,0], encoding: .utf8)!) ? completionO.count : descriptz.count))"
   }
        super.viewWillAppear(animated)
    }

}
